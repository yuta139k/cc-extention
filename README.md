# cc-extention

Claude Code の開発体験を改善する CLI ツール。外部依存なし。

## 機能

### 1. コマンドリスク解説（PreToolUse フック）

Bash コマンド実行前にリスクレベルと解説を表示します。

```
🔴 HIGH RISK: [rm -rf] ファイル/ディレクトリを再帰的に完全削除します。取り消し不可。
🟡 MEDIUM RISK: [npm install] パッケージをインストールします。node_modules と package.json が変更されます。
```

- **HIGH**: `rm -rf`, `git push --force`, `sudo`, `DROP TABLE` 等 → 確認を要求
- **MEDIUM**: `npm install`, `git push`, `mv`, `docker run` 等 → 確認を要求
- **LOW**: `ls`, `cat`, `git status` 等 → 自動許可
- 複合コマンド（`&&`, `||`, `;`, `|`）は分解して最も高いリスクを採用
- 80+ のパターンを収録
- 日本語/英語を `LANG` 環境変数で自動判定

### 2. スクリーンショット連携（`/ss` コマンド）

スクリーンショットを撮影して `/ss` と入力するだけで、Claude が画像を認識して作業を進めます。

```
/ss              → 最新スクショを読み込み、内容から必要な作業を判断
/ss ここ直して    → スクショを見て修正に着手
/ss これ何？      → スクショの内容を説明
```

macOS / Windows / Linux 対応。

## インストール

```bash
npx cc-extention
```

以下が自動で行われます:

1. `~/.cc-ext/hooks/` にフックスクリプトを配置
2. `~/.claude/commands/ss.md` に `/ss` コマンドを配置
3. `~/.claude/settings.json` に PreToolUse フックを登録

インストール後、**Claude Code を再起動**してください。

## アンインストール

```bash
npx cc-extention --uninstall
```

全てのファイルと設定エントリをクリーンに削除します。既存の設定は保持されます。

## 動作要件

- Node.js >= 18
- [Claude Code](https://claude.ai/code)

## ファイル構成

```
cc-extention/
├── bin/
│   └── cc-ext.js              # CLI エントリポイント（install/uninstall）
├── hooks/
│   └── explain-command.js     # PreToolUse フック本体
├── lib/
│   └── command-rules.js       # コマンドリスクルール定義（80+ パターン）
├── commands/
│   └── ss.md                  # /ss スラッシュコマンド定義
└── package.json
```

## リスクルールのカスタマイズ

`lib/command-rules.js` を編集してパターンを追加できます:

```javascript
// high または medium 配列にルールを追加
{ pattern: /^your-command\s+/, name: 'your-command',
  ja: '日本語の説明',
  en: 'English description' },
```

インストール済みの場合は `~/.cc-ext/hooks/command-rules.js` を直接編集するか、再インストールしてください。

## ライセンス

MIT
